{
    "tasks": [
        {
            "command": [
                "docker build -f ${DOCKERFILE} -t ${TAG} --secret ${SECRET} ${CONTEXT}",
                "&& docker run --rm --mount ${DIST_MOUNT} --env-file ${ENV_FILE} ${TAG}"
            ],
            "group": "build",
            "label": "Build: CMD",
            "options": {
                "env": {
                    "CONTEXT": "cmd",
                    "DIST_MOUNT": "type=bind,source=./cmd/dist,target=/output",
                    "DOCKERFILE": "docker/cmd.Dockerfile",
                    "ENV_FILE": "./cmd/.env",
                    "SECRET": "id=env_file,source=./cmd/.env",
                    "TAG": "ryujinxkit:cmd"
                }
            },
            "problemMatcher": [],
            "type": "shell"
        },
        {
            "command": [
                "docker build -f ${DOCKERFILE} -t ${TAG} ${CONTEXT}",
                "&& docker run --rm -p ${PORT_BIND} --env ${ENV} -v ${VOLUME} ${TAG}"
            ],
            "label": "Start Server",
            "options": {
                "env": {
                    "CONTEXT": "server",
                    "DOCKERFILE": "docker/server.Dockerfile",
                    "ENV": "CONTENT=/ryujinxkit-server/content.tar",
                    "PORT_BIND": "8000:8000",
                    "TAG": "ryujinxkit:server",
                    "VOLUME": "ryujinxkit-server:/ryujinxkit-server:ro"
                }
            },
            "problemMatcher": [],
            "type": "shell"
        },
        {
            "command": "docker build -f ${DOCKERFILE} -t ${TAG} ${CONTEXT}",
            "group": "build",
            "label": "Build: CMD (Dev)",
            "options": {
                "env": {
                    "CONTEXT": "cmd",
                    "DOCKERFILE": "docker/development/cmd.Dockerfile",
                    "TAG": "ryujinxkit:Dcmd"
                }
            },
            "problemMatcher": [],
            "type": "shell"
        },
        {
            "command": "docker build -f ${DOCKERFILE} -t ${TAG} ${CONTEXT}",
            "group": "build",
            "label": "Build: Server (Dev)",
            "options": {
                "env": {
                    "CONTEXT": "server",
                    "DOCKERFILE": "docker/development/server.Dockerfile",
                    "TAG": "ryujinxkit:Dserver"
                }
            },
            "problemMatcher": [],
            "type": "shell"
        }
    ],
    "version": "2.0.0"
}
