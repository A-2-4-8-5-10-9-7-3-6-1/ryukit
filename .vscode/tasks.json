{
    "tasks": [
        {
            "command": [
                "docker build -f ${DOCKERFILE} -t ${TAG} --secret ${SECRET_GITHUB_USERNAME} --secret ${SECRET_GITHUB_TOKEN} ${CONTEXT}",
                "&& docker run --rm --mount ${DIST_MOUNT} ${TAG}"
            ],
            "group": "build",
            "label": "Build (Release)",
            "options": {
                "env": {
                    "CONTEXT": ".",
                    "DIST_MOUNT": "type=bind,source=./dist,target=/output",
                    "DOCKERFILE": "docker/release.Dockerfile",
                    "SECRET_GITHUB_TOKEN": "id=github_token,source=./secrets/github_token",
                    "SECRET_GITHUB_USERNAME": "id=github_username,source=./secrets/github_username",
                    "TAG": "ryujinxkit:release"
                }
            },
            "problemMatcher": [],
            "type": "shell"
        },
        {
            "command": "docker build -f ${DOCKERFILE} -t ${TAG} ${CONTEXT}",
            "group": "build",
            "label": "Build (Dev)",
            "options": {
                "env": {
                    "CONTEXT": ".",
                    "DOCKERFILE": "docker/development.Dockerfile",
                    "TAG": "ryujinxkit:development"
                }
            },
            "problemMatcher": [],
            "type": "shell"
        }
    ],
    "version": "2.0.0"
}
